import {Image} from 'react-native';
import styled from 'styled-components/native';
import scale from '../../../utils/scale';

export const BottomTabBarIcon = styled(Image)`
  width: ${scale(24)}px;
  height: ${scale(24)}px;
`;
